name: liquibase-octopus-pipeline
on: push
jobs:
  zipTheArtefacts:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v2
      - name: Check the directory
        working-directory: ./pipeline-octopus
        run: |
          pwd
          ls -ll
